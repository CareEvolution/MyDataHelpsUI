version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 17
        - npm ci
    build:
      commands:
        - npm run build-storybook
  artifacts:
    baseDirectory: /storybook-static/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*